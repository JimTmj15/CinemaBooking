package com.fave.cinemabooking.ui.view_holders

import android.view.ViewGroup
import com.fave.cinemabooking.models.others.Data

class MovieListViewHolder(view: ViewGroup, viewId: Int) :
    GenericViewHolder<Data>(view, viewId) {

    override fun bindViewHolder(data: Data) {
    }
}
